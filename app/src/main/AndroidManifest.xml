<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- CAMERA is the only permission this app actually asks for. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!--
      ML Kit's Play Services dependencies drag INTERNET into the merged
      manifest even though the bundled OCR model never needs it. Strip it, so
      the app has no way to send anything anywhere — the guarantee is enforced
      by the OS rather than by our good intentions.

      ACCESS_NETWORK_STATE is left in place: it only permits *reading* whether
      a network exists, and without INTERNET nothing can be transmitted.
    -->
    <uses-permission
        android:name="android.permission.INTERNET"
        tools:node="remove" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tally">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Tally">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Ships the OCR model with the APK so first scan works offline. -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="ocr" />
    </application>

</manifest>
